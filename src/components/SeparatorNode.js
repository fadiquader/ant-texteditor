import React  from 'react';

const style = {
    border: 'none',
    background: '#5a5a5a',
    minHeight: '3px',
    maxWidth: '50px',
    margin: '20px auto'
}
const  SeparatorNode = () => (
    <hr style={style}/>
);

export default SeparatorNode;